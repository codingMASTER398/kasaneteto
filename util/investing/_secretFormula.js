// the real secretFormula.js is hidden as it is. a secret formula
// to the stock prices

function canIPO(id){
  // Return -1 if the stock cannot IPO yet (in the real game this is if there isn't 2 days worth of data)
  // Return an actual number if it can, and the price will be set to that value.
  return Number
}

function getNewPrice(price, id) { // Current price of the stock, ID of the stock's YT video 
  return Number
}

function initNewPrices(){
  // Called 20 seconds before a stock update
  // You can calculate things here before it goes live, but you don't need to return anything.
  return;
}

module.exports = {
  getNewPrice,
  initNewPrices,
  canIPO
}